import Form from "../features/form/Form"
import BlogPosts from "../features/form/BlogPosts"
const Main = () => {
  return (
    <main>
        <Form/>
        <BlogPosts/>
    </main>
  )
}

export default Main